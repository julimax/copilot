name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: HTML Lint
      uses: CyberZHG/html-lint@master
      with:
        args: "**/*.html"